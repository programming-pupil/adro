apiVersion: v1
kind: Service
metadata:
  name: {{ include "adro.fullname" . }}
  labels: {{- include "adro.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "adro.labels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
