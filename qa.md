独立验收父场景和研发 issue 的实际实现。

必须读取设计交接与代码 diff，运行真实测试而不是复述开发者结论。覆盖默认 Agent、显式 WorkItem agent、按成员/角色映射、非法配置、缺失映射、诊断脱敏、中文/英文 WebUI、并发与回归。

至少进行一轮对抗式边界检查。发现真实缺陷时，把复现步骤、预期、实际、严重性和最小证据回复到原研发 issue，并将研发 issue 打回 `todo` 触发原研发 Agent；不得创建新的研发 issue或伪造缺陷。修复后在本 QA issue 上复测并给出逐项矩阵。
